---
import Layout from '~/layouts/PageLayout.astro';
import HomaIRHesaplayici from '~/components/widgets/HomaIRHesaplayici.astro';
import { client, urlFor } from '~/lib/sanity';
import { PortableText } from 'astro-portabletext';

// Sanity'den "İnsülin Direnci"ne ait içeriği çekiyoruz
const data = await client.fetch(`*[_type == "calculatorPage" && title == "insulin-direnci"][0]`);

const metadata = {
  title: data?.pageTitle || 'İnsülin Direnci (HOMA-IR) Hesaplama',
};
---

<Layout metadata={metadata}>
  <section class="px-4 py-16 sm:py-20">
    <div class="mx-auto max-w-2xl">
      <div class="mb-10 text-center">
        <h1 class="mb-4 text-4xl font-bold md:text-5xl">{data?.pageTitle}</h1>
      </div>

      <HomaIRHesaplayici />

      {data?.content && (
        <div class="prose dark:prose-invert max-w-none mt-12">
          <PortableText value={data.content} />
        </div>
      )}
    </div>
  </section>
</Layout>