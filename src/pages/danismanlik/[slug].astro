---
import Layout from '~/layouts/PageLayout.astro';

// 1. BU BÖLÜM: HANGİ SAYFALARIN OLUŞTURULACAĞINI TANIMLAR
// Astro'ya diyoruz ki, aşağıdaki listedeki her bir eleman için bir sayfa oluştur.
export async function getStaticPaths() {
  const danismanliklar = [
    { slug: 'kilo-alma', title: 'Kilo Alma Danışmanlığı' },
    { slug: 'kilo-verme', title: 'Kilo Verme Danışmanlığı' },
    { slug: 'kilo-koruma', title: 'Kilo Koruma Danışmanlığı' },
    { slug: 'hastaliklarda-beslenme', title: 'Hastalıklarda Beslenme' },
    { slug: 'gebelikte-beslenme', title: 'Gebelikte Beslenme' },
    { slug: 'emzirme-doneminde-beslenme', title: 'Emzirme Döneminde Beslenme' },
    { slug: 'cocukluklarda-beslenme', title: 'Çocukluklarda Beslenme' },
    { slug: 'ergenlikte-beslenme', title: 'Ergenlikte Beslenme' },
    { slug: 'anti-inflamatuar-beslenme', title: 'Anti-İnflamatuar Beslenme' },
    { slug: 'vegan-vejeteryan-beslenme', title: 'Vegan ve Vejeteryan Beslenme' },
  ];

  return danismanliklar.map(({ slug, title }) => {
    return {
      params: { slug: slug },
      props: { title: title, description: `${title} hakkında detaylı bilgi.` },
    };
  });
}

// 2. BU BÖLÜM: OLUŞTURULAN SAYFANIN İÇERİĞİNİ BELİRLER
// Yukarıdaki listeden gelen bilgileri burada kullanıyoruz.
const { title, description } = Astro.props;

const metadata = {
  title: title,
  description: description,
};
---

<Layout metadata={metadata}>
  <section class="px-4 py-16 sm:py-20">
    <div class="mx-auto max-w-4xl">
      <h1 class="mb-4 text-center text-4xl font-bold md:text-5xl">{title}</h1>
      <p class="text-center text-xl text-muted">
        Bu sayfanın içeriği yakında eklenecektir. Şu an sadece bir şablon sayfasıdır.
      </p>
      </div>
  </section>
</Layout>